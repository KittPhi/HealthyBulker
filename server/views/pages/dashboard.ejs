<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>User Profile</title>
    <link rel="stylesheet" href="/css/bootstrap.min.css" />
  </head>
  <body>
    <div class="container">
      <h1>Welcome to Your Profile, <%= user.name %>!</h1>
      <h2>Your Weight, Body Fat, and Muscle Timeline</h2>
      <table class="table table-bordered">
        <thead>
          <tr>
            <th>Date</th>
            <th>Weight (lbs)</th>
            <th>Body Fat (%)</th>
            <th>Muscle (%)</th>
          </tr>
        </thead>
        <tbody>
          <% user.weightEntries.forEach(entry => { %>
          <tr>
            <td><%= entry.date.toDateString() %></td>
            <td><%= entry.weight %></td>
            <td><%= entry.bodyFat %></td>
            <td><%= entry.muscle %></td>
          </tr>
          <% }); %>
        </tbody>
      </table>
      <a href="/" class="btn btn-primary">Go back to Home</a>
    </div>
  </body>
</html>
